import { Watch } from "react-loader-spinner";
import Form from "./Form";
export default function Cities() {
  return (
    <Watch
      height="80"
      width="80"
      radius="48"
      color="white"
      ariable="watch-loading"
      wrapperStyle={{}}
      wrapperClassName=""
      vissible="{true}"
    />
  );
}
